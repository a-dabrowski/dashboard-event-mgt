import React from 'react';
import styled from 'styled-components';

export const ComponentTitle = styled.h5`
  font-size: 18px;
  color: #fff;
  margin: 0;
  font-weight: 800;
  text-transform: uppercase;
  text-align: left;
`;

const Title = {
  ComponentTitle: styled.h5`
  font-size: 18px;
  color: #fff;
  margin: 0;
  font-weight: 800;
  text-transform: uppercase;
  text-align: left;
`,
};

export default Title;
